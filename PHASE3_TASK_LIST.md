# 第三阶段开发任务清单 (Roadmap)

## 阶段一：数据基石 (Data Foundation)
- [ ] **任务 1.1**: 编写 `database_builder.py` 脚本。
    - [ ] 遍历 `HowToCook` 目录。
    - [ ] 解析 Markdown 结构（原料、步骤、图片）。
    - [ ] 建立标签体系（根据文件夹名称映射菜系/分类）。
    - [ ] 输出 `assets/data/recipes.json` 和 `assets/data/tags.json`。
- [ ] **任务 1.2**: 扩展标签库。
    - [ ] 手动补充 `HowToCook` 缺失的维度（口味、营养、心情）。
    - [ ] 为每个标签定义 UI 属性（颜色、图标/形状路径）。

## 阶段二：UI 视觉升级 (Visual Upgrade)
- [ ] **任务 2.1**: 重构 `Bubble` 实体类。
    - [ ] 支持自定义形状（不仅仅是圆形）。
    - [ ] 支持内部动画（如蒸汽、光效）。
- [ ] **任务 2.2**: 实现“老虎机”/“AI 思考中”过渡动画。
    - [ ] 设计加载状态 UI。
- [ ] **任务 2.3**: 优化结果页。
    - [ ] 支持 AI 生成图片的展示。
    - [ ] 增加“搭配推荐”气泡冒出效果。

## 阶段三：AI 逻辑集成 (AI Integration)
- [ ] **任务 3.1**: 移植/重写 `AiService` (Dart版)。
    - [ ] 定义 Prompt 模板（参考 `what-to-eat`）。
    - [ ] 集成 LLM API 调用。
- [ ] **任务 3.2**: 实现推荐算法。
    - [ ] 基于标签匹配度的简单的加权推荐算法。
    - [ ] 引入用户画像（User Profile）权重。

## 阶段四：服务路径实现 (Service Implementation)
- [ ] **任务 4.1**: 完善“自制”详情页。
    - [ ] 渲染 Markdown 菜谱。
    - [ ] 添加计时器、勾选原料功能。
- [ ] **任务 4.2**: 搭建“到店/外卖”模拟页。
    - [ ] 设计餐厅卡片 UI。
    - [ ] 设计外卖下单引导 UI。

## 阶段五：持久化与优化 (Persistence & Polish)
- [ ] **任务 5.1**: 实现收藏功能（SQLite/Hive）。
- [ ] **任务 5.2**: 性能优化（大量气泡的渲染性能）。

---
**当前状态**: 准备启动 **任务 1.1**。

import 'package:flutter/material.dart';

import 'app_tokens.dart';

class AppColors {
  // Primary Gradients
  static const Color sunsetOrange = AppPalette.yolk;
  static const Color goldenHour = AppPalette.chili;

  static const LinearGradient primaryGradient = AppPalette.appetiteGradient;

  // Accents
  static const Color freshLime = AppPalette.herb;
  static const Color berryPop = Color(0xFFE94A6A);

  // Backgrounds
  static const Color lightBackground = AppPalette.broth;
  static const Color darkBackground = AppPalette.night;

  // Text
  static const Color textPrimary = AppPalette.ink;
  static const Color textSecondary = AppPalette.inkMuted;

  // Glass
  static const Color glassWhite = Color(0x4DFFFFFF);
  static const Color glassBlack = Color(0x4D000000);
}
